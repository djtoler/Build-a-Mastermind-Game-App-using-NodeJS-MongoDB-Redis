const modules = require("../module-index/module-index");
const middlewares = require("../middlewares/middlewares-index/middleware-index");
const routers = require("../api/routers/routers-index");

module.exports = {modules, middlewares, routers}